<div data-role="header">
  <% if current_user %>
     <h1>Hospital List</h1>
     <%= link_to "Logout", '/users/sign_out', "data-rel"=>"dialog", "class"=>"ui-btn-right" %> 
  <%else%>                          
    <h1>Authentication Required</h1>
    <%= link_to "Login", '/users/sign_in', "data-rel"=>"dialog","class"=>"ui-btn-right" %> 
  <%end%>
 </div>

<script type="text/javascript" charset="utf-8">
  <%=raw(alert_user)%>
</script>
 <div data-role="content">
   <ul data-role="listview">
           <% if current_user %>
             <%  cookies[:user] = current_user.username %>
             <%  cookies[:last_name] = current_user.last_name %>
             <%  cookies[:first_name] = current_user.first_name %>
              <li> <%= link_to "Patients", '/patients'  %> </li>
              <li> <%= link_to_if supported_browser, "Calendar", calendar_path, {"rel"=>"external"} %> </li>
              <li> <%= link_to_if supported_browser, "Reports", charges_path, {"rel"=>"external"}  %> </li>
              <li> <%= link_to 'Profile', '/users/edit'  %> </li>
              <li> <%= link_to_if current_user.is_admin ,"Admin", {:controller => "admin", :action => "index"}, {"rel"=>"external"} %> </li>
           <%end%>
  </ul>
</div>

<div data-role="footer">
  <h4>Copyright &copy; 2010</h4>
</div> <!-- /footer -->